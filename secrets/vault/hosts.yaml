# Stage 1: start Vault only (vault-dev must be healthy before demo-app can be provisioned)
# Stage 2: after seeding Vault (see seed-vault.sh), uncomment demo-app and reconcile again
apiVersion: gitops.podcd.io/v1
kind: Host
metadata:
  name: local
spec:
  applications:
    - vault-dev
    # - demo-app   # uncomment after running seed-vault.sh
